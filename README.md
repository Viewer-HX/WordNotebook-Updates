# WordNotebook Updates

Public, token-free update metadata for the private WordNotebook application repository.

- `latest.json` describes the latest published macOS release.
- The application only checks this file and opens the release page after user confirmation.
- No application data is collected or uploaded here.
